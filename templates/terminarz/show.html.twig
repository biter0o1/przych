{% extends 'base.html.twig' %}

{% block title %}Terminarz{% endblock %}

{% block body %}
    <h1>Terminarz</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ terminarz.id }}</td>
            </tr>
            <tr>
                <th>Data</th>
                <td>{{ terminarz.data }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('terminarz_index') }}">back to list</a>

    <a href="{{ path('terminarz_edit', {'id': terminarz.id}) }}">edit</a>

    {{ include('terminarz/_delete_form.html.twig') }}
{% endblock %}
